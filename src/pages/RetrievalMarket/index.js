import React from "react";
import { connect } from "react-redux";

function RetrievalMarket() {
  return <div>RetrievalMarket</div>;
}

const mapStateToProps = (state) => ({});

const mapDispatchToProps = (dispatch) => ({});

export default connect(mapStateToProps, mapDispatchToProps)(RetrievalMarket);
